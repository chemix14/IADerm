package com.example.iaderm;

/**
 * UI states for capture validation flow.
 */
public enum CaptureUiState {
    SEARCHING,
    ADJUST_LIGHT,
    ADJUST_DISTANCE,
    READY,
    CAPTURING
}
